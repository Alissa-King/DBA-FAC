# Disabled: conflicts with pages.yml (same concurrency group).
# pages.yml deploys docs/ directly without Jekyll.
name: Jekyll (disabled)
on:
  workflow_dispatch:
    inputs:
      noop:
        description: 'This workflow is disabled'
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'disabled'
